// 객체

const fruit = {
    name: "Apple",
    count: 5,
};

console.log(fruit.name);

// 배열

let fruits: string[];
fruits = "orange"; // error
fruits = ["orange", 1]; // error
fruits = ["orange", "melon"];
